{
  "name": "@frostui/registry",
  "version": "0.1.0",
  "private": true,
  "description": "FrostUI component registry and agent metadata",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./components/*": "./src/components/*.json"
  },
  "scripts": {
    "build:registry": "node ./scripts/build-registry.mjs",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
