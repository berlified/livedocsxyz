{
  "name": "input",
  "title": "Input",
  "category": "forms",
  "description": "Single-line text field with focus ring and disabled states.",
  "dependencies": [],
  "registryDependencies": ["utils"],
  "files": ["registry/frostui/ui/input.tsx"],
  "examples": ["default", "disabled"],
  "variants": ["default"],
  "keywords": ["input", "textfield", "form", "search"],
  "props": [
    { "name": "type", "type": "string", "default": "\"text\"", "description": "Native input type" },
    { "name": "disabled", "type": "boolean", "default": "false", "description": "Disables the input" },
    { "name": "placeholder", "type": "string", "default": "undefined", "description": "Placeholder text" }
  ],
  "ai": {
    "purpose": "Collect single-line text input in forms and filters.",
    "useWhen": [
      "Search fields",
      "Simple form inputs",
      "Inline filters"
    ],
    "preferOver": [
      "Unstyled native inputs in product UI",
      "Contenteditable divs for plain text"
    ],
    "avoid": [
      "Multi-line content (use textarea when available)",
      "Inputs without labels in forms"
    ],
    "compositions": [
      "Input inside Card forms",
      "Input + Button search bars",
      "Input groups with icons"
    ]
  }
}
